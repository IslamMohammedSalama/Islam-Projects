#include <stdio.h>
#include <cs50.h>

int main(void) {
    int a = get_int("a: ");
    int b = get_int("b: ");
    
    if (a > b) {int a = get_int("a: ");
    int b = get_int("b: ");
    
    if (a > b) {
        printf("a > b\n");
    }
    else if (a < b) {int a = get_int("a: ");
    int b = get_int("b: ");
    
    if (a > b) {
        printf("a > b\n");
    }
    else if (a < b) {
        printf("a < b\n");
    }
    else {
        printf("a = b\n");
    }
        printf("a < b\n");
    }
    else {
        printf("a = b\n");
    }
        printf("a > b\n");
    }
    else if (a < b) {
        printf("a < b\n");
    }
    else {
        printf("a = b\n");
    }
}